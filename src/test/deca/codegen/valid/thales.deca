// Description:
// Dans cet exemple, nous utilisons les variables a1, a2, b1, b2, c1 et c2 pour représenter les longueurs de différents segments.
// Ensuite, nous vérifions si les longueurs sont proportionnelles en utilisant l'expression (a1 / a2 == b1 / b2) && (b1 / b2 == c1 / c2).
// Cette expression évalue si le rapport entre les longueurs a1 et a2 est égal au rapport entre les longueurs b1 et b2,
// et si ce dernier rapport est égal au rapport entre les longueurs c1 et c2.

// En fonction du résultat, nous affichons un message approprié en utilisant une instruction if-else.
// Si thales est évalué à true, nous affichons "Les longueurs sont proportionnelles selon le théorème de Thalès".
// Sinon, nous affichons "Les longueurs ne sont pas proportionnelles selon le théorème de Thalès"

{    
    float a1 = 3.0;
    float a2 = 5.0;
    float b1 = 2.0;
    float b2 = 4.0;
    float c1 = 6.0;
    float c2 = 10.0;

    // Vérification du théorème de Thalès
    boolean thales = (a1 / a2 == b1 / b2) && (b1 / b2 == c1 / c2);

    if (thales) {
        println("Les longueurs sont proportionnelles selon le théorème de Thalès.");
    } else {
        println("Les longueurs ne sont pas proportionnelles selon le théorème de Thalès.");
    }
}